class Quiz {
  constructor(quizName, quizId, playerNames, isFinished, image) {
    this.quizName = quizName;
    this.quizId = quizId;
    this.playerNames = playerNames;
    this.isFinished = isFinished;
    this.image = image;
  }
}

export default Quiz;
